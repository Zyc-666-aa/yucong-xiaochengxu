# LUSH 微信小程序候选工程

本目录为原生迁移候选版本，AppID 为 wx8906b179a8517ac4。游客模式可用于本地导入和模拟器预览；后台上传需要在微信开发者工具中使用完整微信账号登录。2026-09-13 已使用正式登录账号上传版本 0.1.0，尚未提交审核或发布线上版本。

开场视频 -> 银色 LUSH 与穹顶 -> 上滑进入 -> 探索更多 / 有趣事物 -> 开始 -> 作品。后续提供作品详情、设计思路、素材档案、关于和可关闭信息层；探索栏中设计思路置于第一位。身份为设计师 + AI 创作者；思路页只保留文字和连接关系。联系入口读取 `portfolio/data.js` 的 `contact` 配置，提供拨号、复制电话、复制微信号和复制邮箱；分享能力保留为微信页面级回调，不在联系弹层中占用一行。简历预览已显示张誉聪 / Albert Lush、设计师 + AI 创作者、平面设计 / UI设计 / VIBE CODING、简介与联系方式，并支持打开文本摘要或复制摘要；正式履历、项目结果和 PDF 文件待资料确认后替换。

欢迎场景复用原网页着色器、24 个贴纸实例及运动公式，纹理来自原 SVG。Canvas WebGL 执行连续动画，按钮与页面使用原生组件。未绘制假的时间、电池与信号栏。作品页采用系统中文无衬线回退链、三层文字层级、分隔线分类，三个当前项目使用统一的纵向大卡片几何；设计作品筛选显示贝曜与石湾玉冰烧，VIBE CODING 筛选显示 LUSH 小程序。作品集分包的非首页页面统一使用银色膨胀 LUSH 字标图片，详情页保留返回作品入口。关于页提供减少动效及重播入口。

2026-09-08 验收：浏览器全局旅程、三种手机视口交互、焦点与 Escape、图片失败兜底、原生静态门禁均通过；微信自带 WXML 与 WXSS 编译器静态检查通过。共享场景在 Chrome 四种视口下完成截图和拖动检查，WebGL 错误码为 0。它们不是微信模拟器或真机截图。

同步预览：运行 `node work/lush-live-preview-server.cjs`，然后打开 `http://127.0.0.1:8769/outputs/LUSH-mini-preview.html`。预览复用候选工程的图片资源，监听 `outputs` 与 `miniprogram-candidate` 的 HTML、CSS、JS、WXML、WXSS 和 JSON 变化并自动刷新；它用于视觉与交互校对，原生小程序仍以本目录为准。浏览器入口 `outputs/index.html` 与 `outputs/LUSH.html` 已统一跳转到 `outputs/LUSH-zh.html`，旧版文件保存在 `work/legacy-entrypoints/` 以便回退。

2026-09-08 已完成开发者工具扫码登录，官方命令行 `islogin` 返回 `true`，当前工程窗口保持响应。原生模拟器完整流程、iPhone 与 Android 真机预览尚未完成，不标记最终交付。此前游客导入修改的 AppID 已在源配置恢复。

基线备份：D:/xwechat_files/CodexData/LUSH/2026-09-07-baseline。原 outputs/LUSH-zh.html 和旧 miniprogram 目录保留，回退可直接打开原目录。测试脚本在上一级 work 目录。

设计方法参考 Emil Design Engineering，MIT，安装登记提交 d23d7f88a2e21c9e4b1418c7abe420f5c1052ba7。两张新增实验图片由图像生成工具生成，不代表客户项目或已完成影片。原生运行时依赖固定为 GSAP 3.15.0；其标准免收费许可仍需按具体发布条件核对。

2026-09-08 流星点缀：欢迎页与作品集背景各保留两条极小、低亮度、低频流星，装饰层不拦截触摸，减少动效时隐藏；详情页不叠加流星以保持阅读稳定。

2026-09-09 综合整改：原生与 HTML 已同步 Intro 错误提示及重试、图片失败兜底、动态辅助文案、联系动作、简历摘要、分享入口、卡片元数据和关于页装饰强度。原生运行时通过 `utils/runtime.js` 兼容旧基础库的窗口、安全区和胶囊信息；底部 Dock 改为边界盒并把安全区计入高度；Intro 缓存键升级为 `lush-intro-v2.mp4`。原生真机验收仍按 `work/NATIVE-DEVICE-CHECKLIST.md` 执行。

2026-09-09 欢迎球材质：参考 `gakkii415/liquid-note-webgpu` 的公开 WebGPU/WGSL 材质方法，在现有 WebGL 球体中加入低幅折射、Fresnel 边缘、窄高光和微细表面层；网页预览以 CSS 多层冷灰蓝渐变保持一致。参考提交、许可证和复用边界记录在 `scene/source-manifest.json` 与 `work/LUSH-SPHERE-MATERIAL-20260909.md`。未复制上游 shader 或素材，真机 shader 编译和帧率仍待验收。

2026-09-09 关于页装饰动效：HTML 与原生均改为 8 个边缘锚定的低亮度图案，全表面层按错峰淡入后缓慢漂移，背景不拦截触摸，文字层保持在上方；减少动态效果时保留静态装饰。浏览器与原生静态门禁记录在 `work/LUSH-ABOUT-ASTRAL-MOTION-20260909.md`，真机渲染与帧率仍待验收。

2026-09-09 Dock 标签与玻璃球：底部左右图标下方显示“探索”“关于”，中央目录按钮换成低亮度冷灰蓝光学球体，保留原三点导航和触控尺寸；HTML 与原生 WXML / WXSS 已同步。参考 `LerSent001/orb` 的 Fresnel、反射和高光分层方法，未复制上游代码或素材。浏览器、静态原生和视觉基线门禁已通过，原生伪元素、底部安全区与真机触摸手感仍需现场验收。

2026-09-09 Dock 圆形玻璃球动效：中央按钮使用 `assets/dock-glass-orb-v2.png` 透明标准圆形资源，原生图片失败时显示 CSS 十字线；8 秒低幅度呼吸只改变 `transform / opacity`，减少动态效果时停用。材质视觉参考 `LerSent001/orb` 的公开 Fresnel、反射和高光层次，未复制上游代码或素材。详细记录见上一级 `work/LUSH-DOCK-GLASS-ORB-MOTION-20260909.md`。

2026-09-09 作品页眉精简：移除作品页 `SELECTED WORK / 2026` 辅助行，LUSH 字标上移；设计流程、素材档案和关于页的页眉信息保留。HTML 与原生 WXML / WXSS 已同步，详细记录见上一级 `work/LUSH-WORK-HEADER-20260909.md`。

2026-09-09 作品筛选标签：作品页将“平面设计”显示为“设计作品”，内部映射保持原有项目数据和筛选结果；HTML 与原生页面已同步，详细记录见上一级 `work/LUSH-WORK-FILTER-LABEL-20260909.md`。

2026-09-09 联系入口微信号：微信号已更新为 `YuCo44`，复制微信号动作和 HTML 预览保持同步；电话与邮箱保持原值。

2026-09-09 作品筛选标签：首个筛选项显示为“项目经历”，内部映射继续显示全部作品；HTML、原生页面与审计脚本已同步。

2026-09-09 贝曜项目整合：用户提供的 8 份 PDF 已合并为作品页第一张“贝曜”设计作品，原始资料和《贝曜 设计说明.docx》归档在上一级 `work/beyao-source/`，运行包在 `portfolio/assets/beyao/` 中携带压缩 PDF、预览 JPG 和 ImageGen 封面。详情文案已同步设计说明中的汕尾贝雕非遗、造型、色彩和面料内容；HTML 详情可打开 PDF，原生详情可统一预览 8 张版面；详细记录见上一级 `work/LUSH-BEYAO-PROJECT-20260909.md`。

2026-09-15 设计思路页：原“AI 实验”入口改名为“设计思路”，并移到探索栏第一位；移除流程配图，改为一套可复用于不同作品的 9 节点树状流程，分为理解问题、形成方案、验证交付三条分支。每个节点同步展示过程、思考和证据，数据位于 `portfolio/data.js` 的 `designFlow`，网页候选使用同一份内容；旧 `lab` 数据保留用于回滚。

2026-09-15 About 页视觉精简：移除身份区的彩色加号球，保留姓名、身份说明和简历、联系、知识库入口；网页与原生页面同步，底部导航与欢迎页不受影响。

2026-09-09 详情页页眉重排：返回动作保持左侧，LUSH 字标移至右侧；原生 `.detail-nav` 使用 absolute 定位，不使用 fixed viewport 固定。网页同步规则见 `outputs/LUSH-mini-preview.css`。

2026-09-09 贝曜资料预览交互：资料卡片点击后通过 `wx.openDocument` 查看 PDF，先写入 `USER_DATA_PATH` 缓存，不提供直接下载按钮；HTML 预览使用内嵌 PDF 查看层，缩略图以完整比例展示。详情页 LUSH 字标随内容滚动，返回键留在左上角。

2026-09-09 文档查看空白修复：资料卡在 HTML 中直接打开高分辨率图片预览层，避免内嵌 PDF 空白；用户可主动打开 PDF。原生优先 `wx.openDocument`，失败时回退到 `wx.previewImage`。浏览器与静态门禁已通过，微信模拟器和真机仍待现场验收。

2026-09-09 文档预览顶部栏视觉重设计：HTML 预览基于 ImageGen 视觉参考完成冷灰蓝液态玻璃头部，准确文字与控件由 CSS 和真实 DOM 实现；小程序端保留微信系统查看器并共享同一交互语义。浏览器截图通过，真机安全区仍待确认。

2026-09-09 参考图字体复用：HTML 文档预览头部按用户参考图采用干净标题锁定和 CJK 安全字体栈，准确文字继续由真实 DOM 渲染；小程序端保留平台系统查看器。

2026-09-09 贝曜竞赛背书：项目介绍已同步加入用户确认的“新北印象”非遗数字服装设计大赛一等奖信息，未增加未提供的赛事细节。
2026-09-09 贝曜项目元信息：项目卡片底部由“2026 / 完整设计提案”更新为“2026.4 设计项目”，HTML 与原生数据同步。
2026-09-09 贝曜资料设计说明：8 张资料均加入对应的设计思路、过程、思考、问题和不足。HTML 与原生小程序均在图片下方提供说明，原生默认站内预览，PDF 由用户主动打开；完整文案见上一级 `work/LUSH-BEYAO-DOCUMENT-NOTES-20260909.md`。

2026-09-09 关于页玻璃球材质：关于页中央圆球采用冷灰蓝分层玻璃表现，保留完整圆形和实时文字，WXSS 加入内圈高光、外部光晕以及低幅度光泽动效，并响应“减少动态效果”。浏览器预览与静态门禁已通过；微信模拟器和真机仍需确认滤镜合成、帧率与安全区。

2026-09-09 联系弹层精简：联系弹层仅保留拨号、复制电话、复制微信号和复制邮箱；分享作品集与复制小程序路径不再作为弹层列表行，页面级分享回调仍保留。

2026-09-09 资料图片点击放大：资料预览层中的图片可点击进入 `wx.previewImage`，使用微信原生手势查看细节；PDF 仍需主动点击“打开 PDF”。
2026-09-10 石湾玉冰烧品牌延展项目：用户提供的 `张誉聪202484316058.pdf` 作为一个完整的第二设计作品加入“设计作品”，项目标题“石湾玉冰烧”，时间 `2025.10`。项目为广东老字号品牌延展、IP 设计展板与文创物料概念展板，获得企业商业背书和选择性鼓励但没有实际落地，仍是大二学生阶段的概念方案。两页 PDF 在同一详情页中作为“IP设计展板”和“文创物料概念展板”预览，运行包路径为 `portfolio/assets/yuxiaobing/`；HTML 与原生数据同步，浏览器筛选、资料说明、图片放大和 PDF 主动打开流程通过，真机系统查看器仍需确认。
2026-09-10 作品列表精简：作品页当前只展示贝曜和石湾玉冰烧两个设计项目；LUSH、光与形、回声切片从列表隐藏，开场和实验资源仍保留。
2026-09-11 作品案例文字结构优化：贝曜与石湾玉冰烧详情页统一采用“业务背景 → 用户 → 问题 → 策略 → 设计方案 → 结果 → 下一步”，并明确用户段落是设计假设、商业背书不等于落地、学生项目仍有待验证的制作与反馈环节。每段正文下新增“链路”和“我的决策”，分别说明设计从输入到下一步的推进关系，以及当时为何这样取舍；详情顶部新增 `PROJECT SNAPSHOT / 项目摘要` 七字段，覆盖价值、目标、用户、范围、挑战、证据边界和阶段结果；作品列表卡片统一为贝曜的纵向大卡片几何；HTML 与原生数据同步。

2026-09-11 LUSH VIBE CODING 案例化：LUSH 微信小程序原型重新加入作品列表，并按业务背景、用户、问题、策略、设计方案、结果、下一步组织七段详情；每段绑定视觉证据图，详情增加项目摘要、链路、我的决策、制作与参考、设计历程和 AI 使用边界。贝曜与石湾玉冰烧记录 Photoshop、Procreate 及 Pinterest、抖音、小红书等公开平台参考；LUSH 使用 ImageGen 生成 portfolio/assets/lush-process-study.png 作为非事实氛围补充。浏览器与静态门禁通过，真机 WebGL、视频、图片、安全区和触摸仍需现场验收。

2026-09-11 设计过程复盘：三个项目详情的“设计历程与 AI 使用”区新增“犯过的错与复盘”，每个项目用 3 条记录说明偏差、影响与下一轮改法。文案遵守学生项目和证据边界，不编造用户数据、市场结果或商业落地；HTML 与原生数据同步，窄屏改为单列展示。
2026-09-11 知识库索引与过程证据：关于页新增“知识库储备”入口，使用 `portfolio/data.js` 中的精选 Obsidian 索引，按“主题 → 执行卡 → 证据 → 复盘”组织，支持节点之间的 `[[双链]]` 式跳转。原始 Obsidian 知识库和私人笔记仍在本地，不在本候选工程中做全量上传或实时同步。三个项目详情的设计历程新增 5 步过程证据卡，引用运行包中的贝曜展板、石湾玉冰烧展板和 LUSH 原型截图，并把辅助视觉标成辅助视觉。原生层使用 `knowledge` 状态、`selectKnowledgeNode` 和 `backKnowledge` 完成节点浏览；真机安全区、图片解码和层级仍需现场验收。

2026-09-12 开发者工具同步状态：网页预览与 `miniprogram-candidate` 目录的知识库索引、过程证据图、详情文案和原生 WXML / WXSS 已完成文件级同步。首次用 CLI `open --project` 重新打开项目时返回 `INVALID_LOGIN / access_token expired`；用户随后进入官方游客模式，改用 CLI `open-other --project` 成功打开候选工程（退出码 0）。本次只打开本地工程，没有上传或发布；模拟器和真机验收仍需继续记录。

2026-09-12 上传尝试：已按授权尝试上传版本 `0.1.0`，描述为“LUSH 作品集原生候选同步”；CLI 返回 `INVALID_LOGIN / access_token expired`。游客模式没有上传凭据，未执行登录绕过或发布；完整登录后可复用同一命令重试。

2026-09-12 关于页复测：HTML 与原生 8 个装饰实例位置已同步避开文字、链接和偏好设置区；关于页动效、球体、Dock、全局旅程和 24 项原生静态测试通过。真机渲染和触摸仍需现场验收。

2026-09-12 Obsidian 知识库入口：关于页增加“参考库 → 总结 → 蒸馏 → 复用”摘要和“知识库储备 / Obsidian”入口。知识库总览解释参考来源、蒸馏方法和 `主题 → 执行卡 → 证据 → 复盘` 结构，节点使用 `[[双链]]` 继续浏览；网页端提供 `obsidian://open` 和复制索引链接，原生小程序提供复制链接动作。运行包只携带精选索引，不上传原始私人笔记。

2026-09-12 关于页个人介绍精简：移除首屏较长的参考库说明，改为“我是谁、我的优点、我能为团队带来什么”三句短文；知识库入口仍保留在独立链接中。网页与原生 WXML 同步，390 x 844 浏览器实测可见且无横向溢出。

2026-09-12 开场入口精简：网页与原生小程序移除“跳过开场”按钮，Intro 视频自然结束后进入欢迎页；播放失败保留错误提示和重试动作。穹顶、上滑进入、开始、重播和减少动态效果路径保持不变。
2026-09-13 动态效果固定展示：移除 About 页“减少动态效果”入口、存储读写和页面状态；欢迎、About、Dock 与环境动效持续展示。WebGL 不可用或上下文丢失时仅保留技术回退，不作为用户可切换模式。系统 `prefers-reduced-motion` 不会关闭候选页动效，浏览器和原生候选已同步；微信模拟器与真机仍待现场验收。

2026-09-13 微信开发者工具同步：通过官方 CLI `open-other --project` 在游客模式成功打开本工程；随后在正式登录状态下复测 `islogin` 返回 `{"login":true}`，并完成上传。

2026-09-13 上传完成：`miniprogram-liquid-candidate` 已通过官方 CLI 上传到 AppID `wx8906b179a8517ac4`，版本 `0.1.0`，描述为“LUSH 液态玻璃欢迎页与作品集候选同步”。CLI 实际包体为主包 1.4 MB、`portfolio/` 943.4 KB、合计 2.3 MB，编译、代码检查、COS 上传和 `progressSuccess` 均完成。为满足分包限制，预览图和玉冰烧 PDF 使用压缩编码，五个未被当前数据引用的诊断素材移到上一级 `work/miniprogram-liquid-upload-excluded-20260913/portfolio-assets/`；上传前完整工程保存在 `work/miniprogram-liquid-candidate-preupload-20260913-original/`。本次只上传开发版本，未提交审核或发布；微信模拟器及真机验收仍需现场执行。完整记录见上一级 `work/LUSH-WECHAT-UPLOAD-20260913.md`。

2026-09-13 同步打开：先关闭 GitLens 通用项目，再执行官方 CLI `open --project miniprogram-liquid-candidate --port 39735 --lang zh --debug`。活动窗口标题为 `LUSH-native-candidate-10`，日志确认项目路径为当前小程序工程，模拟器正在初始化；登录状态保持 `islogin=true`。

2026-09-15 LUSH 设计过程重做：详情页把微信开发者工具的工程选择和运行窗口放在结果证据最前面，另外两张原生适配图明确标为页面状态补充，不冒充模拟器或真机。新增整体框架和 Figma 页面结构稿，按“Figma 定结构 → GPT-6 Astra 主力实现 → Image 2.5 视觉探索 → 微信开发者工具收口”记录制作链路；参考方向记录 Pinterest、推特、小红书以及用户提供的视频和截图。浏览器与原生详情数据、WXML、WXSS 已同步，新增证据图片位于 `portfolio/assets/lush-process/`。当前仍没有公开上线数据，模拟器、真机和正式上线结果继续单独验收。

2026-09-16 GSAP 原生运行时接入：项目新增固定版本 `gsap@3.15.0` 的 npm 清单和锁文件，并准备 `utils/gsap-native.js` 适配层。适配层只把 GSAP 核心时间轴和普通对象动画同步到 `setData`，不引入依赖浏览器 DOM 的 ScrollTrigger、CSSPlugin 或 React 封装。微信开发者工具构建 npm 后即可在页面逻辑中按需调用；本轮未修改现有页面结构、作品内容或视觉动效。
